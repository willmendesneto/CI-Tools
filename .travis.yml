language: php
script: vendor/bin/phpunit -c tests/phpunit.xml --verbose --coverage-text tests/

php:
	- 5.3
  	- 5.4

before_script:
    - "composer install --dev --verbose"

after_script:
  - "php vendor/phpunit/phpunit/composer/bin/phpunit -c tests/phpunit.xml --testdox tests/"